# ★ .gitignore の # は「行頭でしか」コメントにならない。
#    パターンの後ろに書くと、その文字列ごと1つのパターンになって効かない。
#    (2026-08-27に video/ をこれで取りこぼした)

# --- 生成物。make_video.py で作り直せる
video/
MUJOCO_LOG.TXT
__pycache__/
*.pyc
.DS_Store

# --- 実機の走行ログ
# 2026-08-27 の判断で **追跡する**。実験記録もコードと同じ版管理に置く。
# ★1本498KBのnpzが走行のたびに増える。毎回コミットすると履歴が膨らみ続けるので、
#   区切り(セッション単位・日単位)でまとめてコミットすること。
#   重くなってきたら logs/real/ をここに戻して zip 配布へ切り替える。
