# 実機のロボット開発で学んだこと — 2026-08-24〜27

G1の着座タスクを実機で50本以上走らせた記録から。
**うまくいった話ではなく、外した仮説と壊した箇所の記録**として書く。

---

## 1. 実機の結果は「測っていない変数」が決めていた

事前に測れる量で、着座の成否を説明できたものは事実上ひとつも無い（実機44本）。

```
開始角速度RMS      r = -0.02   完全に無相関
しゃがみ深さ        r = +0.15
膝の左右差(終端)    r = +0.22
|ヨーずれ|         r = +0.25
個々の関節(29個)   |r| 最大 0.32
開始トルク(足首/膝/股)  |r| < 0.15
```

一方、操作者は「立つモードで補助なしに自立できたときは高確率で座れる」と
早い段階で言っていた。**それはどこにも記録されていなかった。**

私は手元にあるデータの中だけで答えを探し続けていた。正しい動きは
「記録されていない変数は何か」を先に問い、**それを記録可能にすること**だった。
UIに[補助]欄を足したのはセッション終盤で、遅すぎた。

椅子と段の寸法（段0.210m / 座面0.378m / 後退0.324m）も、
4日間ずっと巻尺で測られないままだった。

---

## 2. n が増えると効果量は縮む

「しゃがみ深さ65度」という指標を、30本の時点でこう報告した。

```
30本  77% vs 18%  p=0.0024
44本  61% vs 19%  p=0.0065
```

しきい値もデータから選んでいた（55〜75度で振ると p=0.0026〜0.48）。
統計としては素人の誤り（多重比較）で、**報告した時点で自分の主張を過大に見せていた。**

外した仮説の一覧:

| 仮説 | 結末 |
|---|---|
| 開始角速度が甘いのが原因（802の静止待ち） | 成功4本が全部0.20〜0.22で始まっていた。撤回 |
| ヨーずれが主犯 | 膝の左右差は直った（1.23→0.11）が成否は決めない |
| 開始姿勢が違う | 実測で最大0.049rad。ほぼ同一だった |
| 146コマで切れば背もたれ動作が消える | 18.5度→15.3度。消えたのは3度分 |
| 開始姿勢での拒否ガード | 全完走回を弾く判定だった。実装前に撤回 |
| 早期の追従誤差で中止するガード | 完走27本中7本を誤って止める。撤回 |

**提案したガードのうち2つは、実装していたら現場を止めていた。**
「実データに当ててから出す」を守ったことだけが救いだった。

---

## 3. 押しても効かないボタンは、無いより悪い

[補助]のセレクトボックスは、半日のあいだ何も起きていなかった。
`do_POST` の許可リストに `assist` を入れ忘れ、サーバが黙って捨てていた。

```python
elif c in ("arm","start",... "run_task"):   # assist が無い
    self.engine.command(c, a)
self._send(b"ok")                            # 知らないcmdは黙って捨てる
```

画面上はセレクトボックスが動くので、**操作者にも私にも「効いている」ように見えた。**
私はその間に3回「選び忘れです」と指摘した。壊していたのは自分だった。

同じ理由で `yawalign` と `stopframe` も死んでいた。

対処は個別の修正ではなく、**照合の自動化**にした。

```
6c. UIのボタンとサーバの配線
  UIの cmd('X') ↔ CMD_ALLOW ↔ 処理分岐  を突き合わせる
  JSが触る getElementById の id が HTML にあるか
```

**黙って捨てる経路があるインタフェースは、いつか必ず何かを捨てる。**

---

## 4. 「OK」を返す判定は、何を測ったかを書かないと嘘をつく

制御権の到達確認がこうなっていた。

```python
ok = (abs(moved) > 0.3*delta) or (abs(tau) > 0.5)
```

`or` が穴だった。トルクは**誰が指令していても**モータが報告する量で、
重力と摩擦だけで0.5N·mは出る。実際に **実測0.000rad・トルク0.7N·m でOK** を返した。

その時点で `ReleaseMode` は送り終わっている。つまり
**内蔵制御は手を離した／こちらの指令は届いていない＝誰も脚を持っていない。**
実機は膝から崩れた。

同じ構造が `SwitchToUserCtrl(7110)` にもある。`code=0` は
「命令を受け取った」だけで、制御権が移った証拠ではない。

**確認は物理でやる。** 小さく動かして、指令どおり動いてトルクが出たかを見る。
ACKと状態フラグは、それだけでは何も証明しない。

---

## 5. 計測系が壊れていても、もっともらしい値が出る

```
制御周期: 中央1.0ms 最大1.0ms (1000Hz / 規定50Hz)
```

何時間もこう出ていた。実測は20.0ms（50Hz）。
原因は `r[-2]` という**位置指定**で、列を190→393に増やしたときに
別の値（`blend_u`）を指すようになっていた。

「1000Hz」は物理的にありえない値なのに、私はそれを疑わずに他の解析を進めていた。

同種の失敗が他に3つ。

- 途中保存（100コマごと）を掴んで「100/395で中断」と誤報した
- ヨーが±90度付近で縮退するRPY展開でピッチの符号を論じ、
  途中でヨー非依存の式に取り直した
- シムの走行が実機の集計に混ざっていた（`is_sim` を見ていなかった）

**位置ではなく名前で引く。単位と桁を既知の基準に当てる。**
1000Hzも、0.000radでOKも、桁を見れば即座に分かった。

---

## 6. 制御性能を決めるのは環境で、コードではない

```
AC接続   CPU中央 3241MHz  制御ループ 20.0ms (50Hz)
AC未接続 CPU中央 1166MHz  制御ループ 45〜51ms (19〜22Hz)  → 転倒
```

**バッテリー駆動では、ガバナを `performance` にしてもクロックが上がらない。**
ファーム／EC側の電力枠で決まるので、OSの設定では回避できない。

これで5本転倒させた。さらに:

- ガバナは**再起動のたびに `powersave` に戻る**
- Ubuntuの `power-profiles-daemon` が設定を黙って上書きする
- 私自身が解析ツール（torch、全コア）を実機セッション中に走らせて
  ループを19Hzまで落とし、転倒させた

**リアルタイム制御の実行時間予算は、プログラムの性質ではなくシステムの性質。**
コメントに書くのではなく、実行前の判定に入れる。ACが抜けていたら押させない。

---

## 7. 事前に分かる条件と、走ってからしか分からない条件を分ける

「制御ループ45Hz未満なら実行不可」という判定は既に入っていた。
**一度も発火しなかった。** 待機中のコックピットは推論していないので50Hz出るからだ。
遅くなるのは方策が走り出してから。

一方、AC接続は走る前に分かる。だから止められる。

**不可逆な動作の前に、その動作の結果ではなく前提条件を見る。**

---

## 8. 引き継ぎは「空白時間」の設計問題

```
ReleaseMode方式
  [内蔵制御] ━━━━┓
                  ┃ ← 0.20〜0.50秒 誰もモータを持たない
  [方策]          ┗━━━━

UserCtrl(7110)
  [内蔵制御] ━━━━┓
  [方策]      ┏━━┻━━━━     先に流し始めてから切り替える
```

順番がすべてだった。**先に指令を流し、それから解放する。**
逆順にすると、その隙間で35kgが自重で沈む。操作者が毎回手で支えていたのはこれ。

もうひとつ、実機で殴られて理解したこと。

**PD保持はバランスではない。** 各関節を目標角へバネで引くだけなので、
姿勢は保つが倒れかけても何もしない。

```
シム: 参照の開始姿勢でPD保持 → 1.5秒で98度
     ほぼ直立でPD保持       → 1.0秒で29.6度
```

「制御権を取って姿勢を保持してから、落ち着いて方策を開始する」という
当初の設計は、物理的に成立していなかった。

**アクチュエータを誰が持っているかを、遷移の瞬間も含めて全時刻で定義する。**

---

## 9. 失敗した前提条件は、ログではなく制御フローを変えなければならない

到達確認が落ちたとき、旧コードはこうしていた。

```
★指令が届いていません（ログ）
カスタム制御へ引き継ぎ(現姿勢を保持中)  ← そのままIDLEへ
```

**警告を出しながら、成功したかのように先へ進んでいた。**

正しい復帰は、取り直す → それでも駄目なら**別経路で効くコマンド**
（内蔵FSMへのRPC = ダンピング）へ逃がす、だった。
lowcmdが通らない状況でも、RPCは別経路なので効く。

**逃げ道は、壊れている経路とは別の経路で用意する。**

---

## 10. 参照軌道は仕様書である。実装を疑う前にそれを読む

操作者が「エビのように反って勢いで座る」「座ったあと背もたれへ向かう動きがある」
と指摘した。私は両方とも方策の癖だと思っていた。参照を読んだら逆だった。

```
0.52〜2.30秒  沈む+後退  後方+0.323m 骨盤-0.280m  前後傾き 0.0度 ← 直立のまま
2.92〜4.84秒  背もたれへ反る                       前後傾き 18.0度 ← 参照に入っていた
```

- 反りは参照に**無い**。実機が追えていないだけ
- 背もたれへの動きは参照に**ある**
- 7.90秒のうち**4.16秒（53%）が静止**
- `seat_onset` が最終コマ（394）を指していた。実際は115コマ
- **腕14関節が全395コマ 0.000rad 固定**。実機は肘1.21radから始まる

そして方策の出力を見ると:

```
right_shoulder_yaw   |a|平均 0.837  飽和 65.9%  ← 時間の2/3、上限に張り付き
脚+腰                |a|平均 0.285  飽和  1.4%  ← 余裕たっぷり
```

**苦しんでいたのは脚ではなく腕だった。**
骨盤を0.28m落として0.32m後退させながら体幹を垂直に保つのはカウンターウェイトが要る動作で、
参照は腕に何の計画も与えていない。方策は±0.7radの枠で釣り合いを即興し、上限に張り付いていた。

私は数時間、方策・設置条件・引き継ぎを疑っていた。
**追いかけている軌道を先に読めば、最初の30分で見えていた。**

---

## 11. 作業に使う道具も、システムの一部

- 走行中のコックピットを再起動して**実行中の走行を止めた**。
  「制御権を持っていたら中止する」判定は書いていたが、`&&` で繋いでいなかったので
  判定が落ちても kill が走った
- `pkill -f "cockpit.py"` が**自分自身のラッパーシェルにマッチして**2回自殺した
- 再起動でスクラッチパッドが消え、監視スクリプトが失われた
- 作業ディレクトリがハーネスにリセットされ、パッチが黙って別の場所に当たっていた

**運用中のセッションでは、補助ツールも安全設計の対象。**
前提条件が落ちたら止まるスクリプトでなければ、それ自体が危険源になる。

---

## 12. 現場の人の観察を、データが無いことを理由に退けない

操作者の「補助なしで自立できたときは座れる」は、
私のどの解析よりも当たっていた。私はその時点で
「開始角速度が効く」と弱い相関（後に r=-0.02 と判明）を根拠に別のことを言っていた。

**データが無いのは、その仮説が弱い証拠ではなく、計測系の欠落。**
議論するのではなく、記録できるようにするのが正しかった。

---

## 初日に戻れるなら、この順でやる

1. **巻尺で設置を測る**（椅子・段・足位置・向き）。床にマーキングする
2. **操作者の補助の有無を1本目から記録する**
3. **参照軌道を全コマ読む**。方策を疑う前に
4. **全ボタンを一度ずつ押して、サーバに届くことを確認する**
5. **AC電源とCPUガバナを実行前判定に入れる**
6. **到達確認は物理で。ACKと状態フラグは証拠にしない**
7. **列は名前で引く。単位と桁を既知の基準に当てる**
8. 効果量を報告する前に n を増やす。しきい値をデータから選んだら、そう書く
