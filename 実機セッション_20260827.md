# 実機セッション手順 2026-08-27

このフォルダだけを現場PCへコピーすれば動く。中身は自己完結している。

---

## 0. 最初に読む(引き継ぎについて、事実を整理する)

### 8/20の版には確かにバグがあった

`field_20260820/real_robot.py` の `ensure_custom()`:

```python
ReleaseMode()          # ← 先に純正制御を解放
  ... 最大5秒ループ ...
self._stream_on = True # ← そのあとで送信開始
```

送信の初期値は `target_q=0 / kp=0 / kd=0`。純正が手を離した瞬間から
**誰もモータを支えていない**(ダンピングですらなくゼロトルク)。

### ただし 8/24 の版はすでに直っていた(重要)

8/24のイベントログに、こう残っている:

```
09:27:13 準備完了 sit_up_rc_r2 (1.5秒)
09:27:13 姿勢チェック: 傾き 実測3.2度 / 参照0.0度 (差3.2度)
09:27:13 内蔵制御から引き継ぎます(姿勢を保持したまま解放)
09:27:13 指令の到達を確認: OK — 左肩ピッチを+0.060rad指令 → 実測0.049rad / トルク2.2Nm
09:27:13 解放から方策開始まで 0.23秒 (傾き0度)
```

**順序は正しく、指令の到達も実測で確認され、引き継ぎは0.23秒・傾き0度で
完了している。**つまり 8/24 の時点で引き継ぎは成立していた。

**したがって「切替で落ちる」が今も起きているなら、原因は順序ではない。**
§8 の切り分けを順番にやること。

### このフォルダの状態

現行の `real/real_robot.py` は正しい順序(現姿勢保持の送信 → 40ms後に解放)。
さらに 8/24 の現場版にあって手元のリポジトリ版に無かった

- `check_authority()`(指令の到達を実測)
- `run<NN>_設定.json` / `イベント.log` / `run<NN>_*.npz` のログ形式

を復元して入れてある(§9)。**8/24と同じ記録が取れる状態に戻っている。**

### 8/26のレビューで見つかった穴(全部直した)

実機投入前に第三者レビューを掛けたところ、**引き継ぎ・安全停止・LowCmd監視に
実機向けの穴**が見つかった。指摘は妥当で、直すまでは床上でNo-Goだった。
以下はすべて対応済み(`real/_orig/` に旧版を残してある)。

| 指摘 | 何が起きうるか | 対応 |
|---|---|---|
| **UIのE-STOPが即時でない** | 停止指令が50Hzループのキューに積まれる。ReleaseModeや方策読込でループが詰まると、その間E-STOPも安全監視も効かない | HTTPスレッドから `robot.estop()` を直接呼ぶようにした。**実測1.5ms**。加えて一度掛かるとラッチし、解除するまで一切の指令を受け付けない |
| **安全監視が制御ループに相乗り** | ループが詰まると監視ごと止まる | 100Hzの**独立した監視スレッド**を追加。傾き・速度・通信だけを見る |
| **制御ループの例外で全部止まる** | 例外でスレッドが死ぬと、画面は生きたままロボットは最後の目標を保持し、監視も停止 | ループ全体をtry/exceptで囲み、例外は必ずDAMPに落とす |
| **500Hz送信スレッドが死んでも検出できない** | `Write()`の例外を握り潰しており、`healthy()`は受信しか見ていない。**画面は正常なのに指令だけ止まる** | 送信の心拍を持ち、`healthy()`の条件に入れた。死んでいたら自動再起動 |
| **Release失敗でも `custom_active=True`** | 制御権が無いのに「持っている」と表示 | 送信が出ていることを確認してから解放し、`ensure_custom()`が成否を返す。最終判定は従来どおり`check_authority()`の実測 |
| **手動の[custom]が到達確認をしない** | 引き継いだつもりで指令が届いていない | [custom]でも`check_authority()`まで必ずやるようにした |
| **終了時のdampが送られない保証がない** | `set_damp()`はバッファを書くだけ。`close()`は送信を止めてからdampを設定していた(逆順) | `close()`が**dampを実際に送り切ってから**停止するようにした |
| **SDKのRPCが無限に待つ** | 2026-08-20実測でReleaseModeが5分以上返らずUIごと停止 | 全RPCを`_rpc`/`_rpc_async`経由に統一。呼び出し側は絶対にブロックしない |
| **方策出力にガードが無い** | NaNがそのままPDの目標になる | NaN/Infを拒否して直前の目標を保持(3コマ連続でDAMP)。可動域と1ステップ変化量に外枠(§付録B) |
| **`probe_release.py` が壊れている** | 存在しないメソッドを解放後に呼び、**解放したまま例外終了**する | 実行可能に修正。後始末を`finally`に置いた。lowcmdの購読はしない(§7-7) |
| **`logs/` に実行権が無かった** | ログが1本も残らない | 修正済み。`preflight.py`が毎回確認する |

### ★[custom] の正体(いちばん大事な訂正)

**この実装の [custom] は、Unitree純正のバランスモードではない。**
`real_robot.py` の `ensure_custom()` がやっているのは

> 現在角度 `q_now` を目標にした**関節PDだけ**

で、重心制御・接触制御・踏み出し・重力補償は**無い**。切替直後は
`q_target = q_now` なので比例トルクはほぼゼロ、純正バランス制御が出していた
支持トルクは引き継げない。**数秒で沈む・傾くのは、指令が届いていない場合
だけでなく、指令が正しく届いていても起こる。**

`COCKPIT.md` 自身に「方策なしのPD保持は数秒で釣り合いを失う」と書いてあり、
旧版§4の「[custom]を押して10秒放置して観察する」はそれと矛盾していた。
**この手順は削除した(新しい§4フェーズA)。**

---

## 1. 持ち物

### PC・ソフト
- 操縦ノートPC(有線LANポートのあるもの)
- **このフォルダ一式**(`real/` `deploy/` `model/` `motions/`)
- Python 3.10+ と `torch numpy mujoco unitree_sdk2py`
  → 現場でpip installしない。**事前に入れて `--sim` で起動確認まで済ませる**
- LANケーブル、電源アダプタ、USBハブ

### 実機まわり
- **吊り具(ガントリー+ハーネス)** — 今日は全ての試行で必須
- 物理E-STOPを手元に
- 段差ブロック(21〜22cm)、椅子
- **巻尺**(椅子の座面高さ・段からの距離・ヨーを毎回記録する)
- 床マーキング用テープ(開始位置。許容±3cm・ヨー±15度)
- 予備バッテリー

### 記録
- 動画(横から+後ろから。**着座の瞬間の骨盤位置が分かる角度で**)
- 温度メモ用(モータ温度はログに自動で入るが、室温も記録)

---

## 2. ネットワーク設定

```bash
sudo ip link set enp46s0 up
sudo ip addr add 192.168.123.222/24 dev enp46s0
ping 192.168.123.161        # 実測0.1ms台で応答すること
```

NIC名は `ip a` で確認。到達しなければ配線とIPを疑う(方策は無関係)。

---

## 3. 起動

```bash
# ★0. 実機投入前チェック(30秒)。ここがNG 0 でなければ実機に出ない
python3 real/preflight.py --iface enp46s0

# ★0b. 実機の素性チェック(送信しない。ファーム更新後は必須)
python3 real/probe_version.py --iface enp46s0

# ★1. 必ず先にモックで手順を通す(実機不要・5分)
python3 real/cockpit.py --sim

# 2. 実機
python3 real/cockpit.py --iface enp46s0

# 3. 実機ミラー(★別ターミナル。送信しないので同時に走らせてよい)
python3 real/twin.py --iface enp46s0
```

`preflight.py` が見るもの: Python環境(torch/mujoco/unitree_sdk2py)・
ログの書き込み権限・deployの整合(関節順・NaN・meta)・MJCFの関節名対応・
観測次元と推論・**目標ガードの動作**・残留プロセス・NIC/ping。

**「フォルダだけで自己完結」は正確ではない。**方策とモデルは自己完結して
いるが、Python環境は現場PCに入っていること。現場でpip installはしない。

ブラウザで **http://localhost:8090**。画面上部に[SIMモック]/[実機]が出る。

**起動しただけでは制御権を取らない。**接続=観測のみ。
明示的に[custom]または[ARM→開始]を押して初めて引き継ぐ。

---

## 4. 今日の実行順序

### 方策に入る前のモードは `stand`(FSM 4)。walk からは入らない

**結論: `damp` → `stand` → 静止を確認 → [custom] → 方策。**

Unitree公式に「床上で純正balanceから自作方策へ無瞬断で渡す」手順は**存在しない**。
公式の低レベル例は `ReleaseMode()` のあと5秒待ってから publisher を作る
(吊り下げ/横たわり前提)。`unitree_rl_gym` は 吊り下げ→ゼロトルク→
2秒補間→policy、`unitree_rl_lab` は Passive→FixStand(3秒)→RLBase。
**どれも純正balanceから奪う形ではない。**

うちの 2026-08-24 の実測(7走行)。引き継ぎ直後(方策開始時点)の傾き:

| 直前のモード | 回数 | 方策開始時の傾き |
|---|---|---|
| `stand`(FSM 4) | 6 | 0 / 0 / 4 / 1 / 1 / 2 度 |
| `walk`(FSM 200) | 1 | **7 度**(全7走行で最大) |

n=1なので断定はしないが、機序ははっきりしている。**ウォーキングFSMは
内蔵の目標が毎tick変わる**ので、その途中で「いまの関節角」をラッチして
保持すると、**重心移動の途中の姿勢を固めてしまう**。`stand` は目標が
ほぼ静的なので、ラッチした姿勢が釣り合いに近い。

**今回から `stop_move()`(内蔵の速度指令をゼロ)を引き継ぎの前に呼び、
0.3秒待つようにした。**これまで呼んでいなかった。`stand` では無害なので
モードを問わず実行される。イベントログに引き継ぎ直前の角速度が残るので、
静定してから掴めているかを後から確認できる。

やむを得ず walk から入るときは、**歩行を止めて数秒待ち、完全に静止してから**
[custom] を押すこと。

### 今日の切替手順(★旧版から変更。[custom]→10秒放置はやらない)

```
damp → stand(FSM 4) → 完全に静止するまで待つ
     → [1. ARM] または [▶ 単体実行]   ← 方策の読み込みはここ。まだ引き継がない
     → 引き継ぎ(自動) → そのまま方策開始
```

**[custom] ボタンは今日は使わない。**[custom]はPD保持に切り替えるだけで
バランス制御が無く(§0)、押した先に「待つ」時間があるほど危ない。
ARM/単体実行は**重い準備を全部済ませてから**引き継ぎ、引き継いだ直後に
方策を開始する。8/24の実測で引き継ぎ0.23秒・傾き0度。

### フェーズA: ハンドオーバー単体の検証(★吊り下げ・補助者付き・短時間)

**吊り具に体重を預けた状態で**、次を確認する。ここが通らなければ以降は無意味。

1. 実機をリモコンまたは[立つ]で純正のバランス立位にする
2. **完全に静止するまで待つ**(揺れが収まってから)
3. コックピットで **[▶ 座る]**(`sit_up_rc_r2`)を押す
   — 準備 → 引き継ぎ → 到達確認 → 方策開始まで自動で続く
4. **床には降ろさない。**吊ったまま1本走らせて、イベントログを読む

見るのはイベントログの4行。**目視の印象ではなく数値で判定する。**

| ログの行 | 合格の目安 |
|---|---|
| `内蔵制御から引き継ぎます … 角速度` | 0.10 rad/s 未満(静定してから掴めている) |
| `指令の到達を確認: OK — …実測◯rad / トルク◯Nm` | 実測 0.02rad 以上 または トルク 0.5Nm 以上 |
| `解放から方策開始まで ◯秒(傾き◯度)` | **0.5秒未満 / 傾き5度未満** |
| `フェーズ開始/完走` | 完走すること |

| 結果 | 意味 | 次 |
|---|---|---|
| 上の4行が全部合格 | 引き継ぎ成立 | フェーズBへ |
| `★指令が届いていません` が出る | 制御権が移っていない | 開始は自動で中止される。§7 の `mode_pr` / 二重publish を疑う |
| 引き継ぎに0.5秒以上かかる | RPCが詰まっている | 別ターミナルの `probe_lowcmd.py` で送信元を見る |
| `★DAMP: 指令の送信が停止` | 送信スレッドの故障 | ログの `send_err_msg` を残して中止 |

**★[custom] を押して放置する試験はしない。**バランス制御が無いので、
沈む・傾くのは正常な挙動であり、**引き継ぎの成否を切り分けられない。**
切り分けたいのは「指令が届いているか」で、それは `check_authority()` が
上の1行で直接測っている。

**下2つ・3つ目が出たら今日は床上で方策を動かさない。**原因を切り分けて
終わる方が価値がある。

### フェーズB: 着座の本番(椅子は真後ろ)

椅子はロボットの**真後ろ**に置く。毎回、巻尺で以下を記録:
- 座面高さ、段の面からの距離、椅子のヨー、開始位置のマーキングからのずれ

| 順 | パターン | 回数 | 目的 |
|---|---|---|---|
| 1 | `sit_up_rc_r2` | 2 | **アンカー**。前回と同じものを最初に回して、環境が前回と同じか確認 |
| 2 | `sit_up_deep_r2` | 3〜5 | **本命**。深座り版(rc比+6.7cm深い) |
| 3 | `sit_up_rc_r2` | 1 | 終わりにもう一度アンカー(温度上昇の影響を見る) |

**アンカーを最初と最後に入れるのが重要。**前回の「数値では成功」問題は、
比較対象なしで測っていたことも一因。

### フェーズC: 余力があれば

- 登り(`climb_slow_r2`)、旋回(`turn_wide_r2`)
- **受動データ取り**: `python3 real/listen_only.py` で、操作者が手で
  関節を動かしながら記録(世界モデルの学習データになる。制御権を取らない)

---

## 5. 使う方策(★2026-08-26: これまで使った着座方策を全部UIに出した)

**消さない方針に変えた。**比較と再現に要るので全部選べる。ただし実機で
失敗した実績があるものは、**実行前に確認ダイアログでその事実を出す。**
判断は operator に残す。

| パターン | 位置づけ | 備考 |
|---|---|---|
| **`sit_up_deep_r2`** | ✅ 本命 | シム18/20・横転0・rc比+6.7cm深い。「ずり落ち」は50試行中1本(3%)(§6)。**2026-08-26に実機で完走** |
| `sit_up_rc_r2` | ✅ アンカー | 実機較正シムで再学習した版。比較の基準に使う |
| `sit_up_rb_r2` | ✅ | rc の比較対象。95%(実機開始分布込み) |
| `sit_up_r2` | ✅ 原点アンカー | 一番古い基準 |
| `sit_lean_r2` | ○ | 軽い後傾(-34度)。90% |
| `sit_r2` / `sit_patternA_recline24` | ○ | 後傾24度・右ロール17度(記録用。姿勢に癖) |
| `sit_up_rd_r2` | ⚠️ 警告つき | 2026-08-24 の実機で**横に倒れた**(ゲート未通過の暫定版) |
| `sit_up_slow_r2` | ⚠️ 警告つき | 現行条件の再測定で**8回中5回が早落ち**(成功率40%) |
| `climb_slow_r2` / `climb_r2` | ✅ | 慎重に登る版から |
| `turn_wide_r2` / `turn_fine_r2` | ✅ | ワイドスタンス旋回から |

⚠️ の2つは `preflight.py` でも警告として出る(NGにはしない)。

---

## 5b. ★2026-08-26 の実機記録(転倒1件)。ここから何が言えて何が言えないか

| 時刻 | 方策 | 結果 |
|---|---|---|
| 11:06:30 | `sit_up_deep_r2` | **完走**(395コマ) |
| 11:09:20 | `sit_up_rc_r2` | **転倒**(46コマ / 0.9秒で34.6度 → 1.0秒で40度自動DAMP) |

**手順**: [カスタム制御へ] → 約41秒保持 → [▶ 座る]。
つまり §4 で「やらない」と決めた経路を通っている。

### 2本の違い

両方**同じしゃがみ姿勢**から始まっている(膝0.854 / 0.855rad。参照の
開始は0.549rad)。温度も傾きもほぼ同じ。**開始前の状態量では区別できない。**

| 量 | deep(完走) | rc(転倒) | 8/24の完走7本 |
|---|---|---|---|
| 開始の傾き | 5度 | 3.8度 | 0〜4度 |
| 開始の角速度 | — | 0.01 rad/s | 静定済み |
| 脚腰の開始ずれ | 0.354 rad | 0.378 rad | 0.356〜0.382 rad |
| 膝の開始ずれ | 0.305 rad | 0.31 rad | 0.220〜0.336 rad |

**どれも完走回と同じ範囲。**違いが出たのは開始した後:

| | deep(完走) | rc(転倒) |
|---|---|---|
| 1コマ目の膝の指令 | +0.36(穏やかに伸ばす) | **+0.03(ほぼ伸ばしきり)** |
| 0.2秒の脚腰追従誤差 | 0.141 rad | **1.112 rad** |
| 足首ピッチ | — | **act=-0.99 で飽和、可動域(-0.87)外の -0.983 を指令** |

rc は同じ姿勢から**飽和した強い指令**を出し、足首が機械的ストッパへ
突っ込んで前に倒れた。deep は同じ姿勢からもっと穏やかな指令を出した。

### 自動ガードを2つ試して、2つとも撤回した(重要)

| 案 | 検証結果 |
|---|---|
| 開始姿勢が参照から離れていたら拒否 | ❌ **完走回を全部落とす。**腕は完走回でも常に参照から約1.0rad離れている(29軸の最大で見ると全滅) |
| 開始0.6秒の脚腰追従誤差が大きければ中止 | ❌ **完走27本中7本を誤って止める。**しきい値は完走回の最大0.949radより上・転倒回の1.112radより下でないと成立せず、失敗の実データ1本でその15%の窓に置くのは危険 |

→ **数字が足りないうちは止めない。記録して貯める。**
`run<NN>_設定.json` に `start_pose_err`、イベントログに開始直後の
追従誤差が毎回残るようにした。失敗回が数本貯まったら決め直す。

### いま言えること(=今日の手順)

**純正 `stand`(FSM 4)から入ること。**8/24に stand から入った7本は全部
完走している。今日の2本は [custom] で41秒保持したしゃがみ姿勢から入って
1勝1敗。**[custom] はバランス制御が無いので、押した先で待つほど姿勢が
参照から離れる**(§0)。

`sit_up_ds_r2`(深座り×ゆっくり)は学習中。**ゲートを通ったら別途渡す。
通らなければ渡さない。**

---

## 6. 今日いちばん見てほしい現象:「ずり落ち」

シムで再現できた失敗。一度きちんと座ってから、**4.6秒かけて骨盤が7.8cm沈み、
尻荷重が219N→76Nへ抜けていく**(体重が座面から脚へ戻る)。
前回の「完走したのにハーネスが体重を受けていた」はこれの可能性が高い。

**8/24のログにも同じことが起きている。**最初の走行(`sit_up_rc_r2`)は

```
09:27:21 フェーズ完走: sit_up_rc_r2
09:27:23 ★DAMP: 傾き40度        ← 完走の2秒後に倒れている
```

完走したあと2秒で40度傾いて自動DAMPしている。**完走 ≠ 安定。**

**着座後5秒は必ず観察を続ける。**着座した瞬間だけ見て成功と判断しない。

見分け方(現場で目視できる):
- 座った直後の骨盤の高さを覚えておく
- そこから**じりじり下がっていくなら、ずり落ち**
- まともに座れている場合、シムでの沈みは0〜6mm(ほぼ動かない)

**シムでの発生率は 39本中1本(3%)。**沈みの分布は二値で、38本が0〜6mm、
1本だけ67mm(中央1mm / 90%点4mm / 最大67mm)。

→ **今日5回程度なら、予測としては全部きれいに座るはず。**
もし2回3回とずり落ちるなら、**シムが実機を過小評価している**ということで、
それ自体が今日いちばん大きな発見になる。回数と沈み具合を必ずメモすること。

**ハーネスを一瞬だけ緩めて体重が座面に乗っているか確かめる**のが最も確実。
ただし必ず1人が支えられる位置に立つこと。

---

## 7. 絶対にやってはいけないこと

1. **解放してから送信を始めない。**必ず「現姿勢保持の送信 → 後から解放」。
   逆順は必ず脱力する。
2. **`__init__` で `ReleaseMode()` を呼ばない。**接続=観測のみ。
   立位中にアプリを起動した瞬間に脱力する。
3. **E-STOPをキューに載せない。**SDKのRPCを50Hz制御ループの中で呼ばない。
   応答が返らないとループごと止まり、安全監視まで死ぬ。
4. **同じ `rt/lowcmd` に別プロセスからpublishしない。**
   残骸プロセスが500Hzで送り続けて、実機が60分唸り続けた実績がある。
   起動前に必ず確認:
   ```bash
   ps -eo pid,comm,args | grep -E "cockpit|run_fsm|listen" | grep -v grep
   ```
5. **失敗したログを消さない。**失敗ログの方が価値が高い。
6. **静的な前傾から開始しない。**傾きが大きいのに角速度がほぼゼロの状態は
   どの方策でも回復不能(実測0/10)。コックピットが自動で弾くが、
   目視でも「大きく傾いて止まっている」なら一度立て直す。
7. **同一プロセスで `rt/lowcmd` を publish しながら subscribe しない。**
   cyclonedds内部でデッドロックする(2026-08-20 A/B実測: 購読ありで即停止、
   なしで心拍49Hz維持)。lowcmdを見たいときは**別ターミナルで**
   `python3 real/probe_lowcmd.py --iface enp46s0`(送信しないので安全)。
8. **[custom] を押して放置しない。**PD保持だけでバランス制御は無い。
   押すなら、支えたまま速やかに方策を開始するか[ダンプ]へ戻す(§0)。
9. **ソフトのE-STOPを信用しきらない。**1.5msで出るようにはしたが、
   出るのは `rt/lowcmd` の damp であって、モータ電源は切れない。
   **物理E-STOPを手元から離さないこと。**

---

## 8. うまくいかないときの切り分け順

貼っていただいた指摘の順序と同じ。**方策を疑うのは最後**。

1. **制御権の空白** → フェーズAで確認済みのはず
2. **LowCmd の mode / gain 初期化**
   - 全モータの `motor_cmd[i].mode == 1`
   - `mode_machine` が `lowstate.mode_machine` と一致(実測5)
   - **`mode_pr`** ← ここだけ未検証。現行コードは `0` 決め打ち。
     足首のPR(pitch/roll)とAB(並列モータ)を取り違えていないか
   - CRCが毎回更新されている
3. **初回 q / action の不連続**
4. **観測の契約**(関節順・スケール・履歴)
5. **最後に本来のsim2realダイナミクス**

### 症状別の一次容疑

| 症状 | まず疑う |
|---|---|
| 切替と同時に落ちる | 制御権の空白(§0) |
| 最初の1フレームだけ跳ぶ | default pose / action初期値 / 関節マッピング |
| 指令は連続なのに数フレーム後に倒れる | 観測の順番・履歴・位相 |
| 高周波に震える | ゲイン / action scale / 遅延 |
| 四肢が伸びる・激しく痙攣する | 関節順 / 観測順 / PR・AB取り違え |
| 何もしていないのに唸る | 残留プロセス(§7-4) |

---

## 9. 取るログ

コックピットが `logs/real/cockpit_YYYYMMDD_HHMMSS/` に自動保存する。
**形式は 8/24 の現場版と同じものに戻してある**(手元のリポジトリ版は
古い形式で書いており、`log_view.py` / `ab_report.py` が読めなかった)。

```
run01_設定.json            その回の設定(方策・kp/kd・関節名・開始温度・meta)
run01_0_sit_up_deep_r2.npz 毎コマの記録。100コマごとに逐次保存
イベント.log                操作と引き継ぎの時刻つき記録
```

`.npz` の中身: `rec`(T×188)+ `cols`(列名)+ `obs`(T×615)+ `final`。
列は `t / fsm / phase_i / elapsed_s / tilt_deg / q29 / dq29 / tau29 /
quat4 / gyro3 / target29 / act29 / temp29`。

**今回追加した2列**(末尾なので既存ツールに影響しない):

| 列 | なぜ要るか |
|---|---|
| `dt_ms` | ループ周期の実測。Pythonの尾側の遅れが実機で出ているかを**数値で**判断する |
| `ms_infer` | 観測構築+推論の所要時間(参考: 手元の実測は中央1.1ms / 予算20ms) |

**中断した回も必ず保存される**(`final=False` が「途中で終わった」印)。

`run<NN>_設定.json` に **`guard`** が入るようになった(2026-08-26)。
その回に安全ガードが何回効いたかの記録:

| 鍵 | 意味 | 期待値 |
|---|---|---|
| `guard_nan` | NaN/Infを弾いた回数 | **0**。1回でも出たら観測かIMUを疑う |
| `guard_clip` | 可動域+0.75radの外枠で丸めた回数 | **0**。0でなければ壊れた指令が出ている |
| `guard_rate` | 1ステップ1.5radの外枠で丸めた回数 | **0** |
| `guard_over` | 素の可動域を超えたコマ数 | 0でなくてよい(下記) |
| `guard_over_max` | そのはみ出しの最大[rad] | 0.70まで(構造的な上限) |
| `send_n` / `send_err` | 送信パケット数 / 連続失敗数 | err=0 |

`guard_over` は**異常ではない**。`target = ref_q + action×0.70` で、
方策が機械的ストッパへ押し付けている量。シムでも同じことが起きており、
実測(8/20・8/24の全34ログ)でも `climb_slow_r2` で0.66%のコマが最大
+0.54rad はみ出していた。**ガードはここを書き換えない**(数えるだけ)。
外枠を0.75radに置いてあるのは、正常な指令に絶対に触らないため。

確認は現地でもできる:

```bash
python3 real/log_view.py      # 1本ずつの詳細
python3 real/ab_report.py     # 方策ごとの集計(A/B比較)
```

### 走行中に見るもの(実機ミラー :8091)

コックピットの「ビュー」枠に、左=実機の実際の姿勢 / 右=方策が意図した姿勢
が並ぶ。**目で「なんとなく違う」ではなく、数値で見る。**

| 量 | 正常 | 異常なら |
|---|---|---|
| 追従誤差 最大 | 0.2rad 以下 | **0.35rad超が続く** → PDが負けている / 関節マッピング / 二重publish |
| 追従誤差 RMS | 0.05 前後 | 上がり続けるなら負荷か温度 |
| 参照との差 最大 | 〜0.7rad | 0.70radに張り付き = action飽和。方策が限界まで押している |

★ゲインが0のとき(標準モード中・待機中)は追従誤差に意味がない。
画面にもその旨が出る。

### 走行後にその場で見るもの(再生)

```bash
python3 real/twin.py --replay logs/real/cockpit_<日時>/run01_0_sit_up_deep_r2.npz --port 8092
```

記録した毎コマを、実機の姿勢と参照の姿勢で並べて再生する。**実機不要。**
「どこで」「どの関節が」ずれ始めたかを、その場で目と数字の両方で確認できる。
`log_view.py` の数値要約と併せて使う。

### 現場で必ずメモするもの(ログに入らない)

- 椅子の座面高さ / 段からの距離 / ヨー(**毎回**)
- 開始位置のマーキングからのずれ
- ハーネスの張り具合(緩めたか、支えたか)
- 室温、セッション開始からの経過時間
- **失敗したときは「どこで」「どう」崩れたかを一言**

---

## 10. 帰ってからやること

1. `logs/real/` をそのまま持ち帰る(**失敗ログを消さない**)
2. `python3 real/log_view.py` で1本ずつ確認
3. `dt_ms` / `ms_infer` の分布を見て、Python実装で足りているかを判定
4. 着座した回について骨盤高さの推移を見て、**ずり落ちの有無**を数える
5. 世界モデルの学習データへ追加(`wm_dataset.py --real`)
6. **現場PCの `real/` を必ず持ち帰る**(最終版がまだ回収できていない)

---

## 付録B: 安全機構の仕様(2026-08-26版)

### 止まる条件と、止め方

| 条件 | 誰が見ているか | 動作 |
|---|---|---|
| E-STOP(赤ボタン/スペース) | HTTPスレッド(**キューを経由しない**) | 即damp。実測1.5ms |
| 体幹の傾き > 40度 | 監視スレッド(100Hz)+ 制御ループ(50Hz) | 即damp |
| 関節速度 > 32 rad/s | 同上 | 即damp |
| LowState途絶 > 0.2秒 | 同上 | 即damp |
| **lowcmd送信の停止 > 50ms** | 同上(新規) | 即damp |
| 観測/方策出力のNaNが3コマ連続 | 制御ループ | 即damp |
| 制御ループの例外 | 制御ループ(新規) | 即damp |
| 静的な前傾(傾き>15度 かつ 角速度<0.25rad/s) | フェーズ開始時 | **開始を拒否**(dampはしない。現姿勢の保持は続ける) |
| サーバ終了(Ctrl+C) | main | dampを**送り切ってから**終了 |

★**一度止まるとラッチする。**解除は [1. ARM] か [▶ 単体実行] を押したとき
だけ(ログに「E-STOPラッチを解除しました」と残る)。ラッチ中は方策も
補間も一切の指令を受け付けないので、詰まっていた処理が後から古い目標を
書きに来ても上書きされない。画面の「制御権」タイルが `E-STOP` と赤く出る。

### ★dampしてはいけない場面(ここは変えていない)

- **指令が届いていないとき** — dampも届かない。届くなら脱力させることになる
- **静的な前傾で開始を拒否したとき** — 機体は自前PDで立っている。
  kp=0にすればその瞬間に崩れる
- **check_authority の最中** — 試験する関節以外は現姿勢で保持したまま。
  2026-08-20に他を全部kp=0にして膝が167度まで沈んだ

### 目標ガード(実機へ出る指令の最後の関所)

`set_target()` が唯一の入口。**ゲイン(kp/kd)には絶対に触らない** —
崩れかけの機体から支持力を奪うガードは、事故そのものになる。

| ガード | しきい値 | 根拠 |
|---|---|---|
| NaN/Inf | 拒否して直前の目標を保持 | 3コマ連続でDAMP |
| 可動域の外枠 | 可動域 ±0.75 rad | 正常な指令のはみ出しは構造的に0.70radが上限。0.75なら正常時に絶対に掛からない |
| 1ステップの変化量 | 1.5 rad/step | 参照の計算上最大0.49、実機ログ実測最大0.80。正常時に掛からない |

---

## 付録C: ファームウェアを上げた場合(★上げたなら再検証が要る)

このコックピットは、**実測して決めた値**にぶら下がっている。
ファームウェアはこれらの前提を変えうるので、上げた直後は8/24の実測を
そのまま信用しない。

上げたら、方策の前に必ずこの順で確認する:

```bash
python3 real/probe_version.py --iface enp46s0  # ★これ1本で下の前提を全部読む
python3 real/probe_lowcmd.py  --iface enp46s0  # 誰が何Hzで指令を出しているか
```

#### 2026-08-26 の実測(ファーム更新後・実機接続で確認済み)

| 項目 | 実測 | 判定 |
|---|---|---|
| LowState 受信 | 1042 Hz | OK |
| `mode_machine` | 5 | 8/24と一致 |
| `mode_pr` | **0** | ★§8-2の未検証項目。**PRで確定** |
| モータ29軸 `mode` | 全軸 1(有効) | OK |
| `CheckMode` | `{'form':'0','name':'ai'}` | 内蔵制御が動作中 |
| FSM ID / MODE | 1 / 0 | damp待機 |
| 立ち高さ / スイング高さ | 0.560 m / 0.060 m | 記録 |
| `LowState.version` | `[0, 0]` | **ファームは埋めてこない**。版数はUnitreeアプリ側で控えること |


確認する前提:

| 前提 | 現在の実測値 | 変わっていたら |
|---|---|---|
| `LowState.mode_machine` | 5 | そのまま返すので自動追従。値は起動ログで確認 |
| `mode_pr` | 0(PR) | 足首の意味が変わる。§8-2 の未検証項目 |
| FSM id: ゼロトルク/damp/stand/walk | 0 / 1 / 4 / 200 | ボタンが効かない・別動作になる |
| `ReleaseMode()` の応答時間 | 詰まることがある | `probe_release.py` で測り直す |
| `rt/lowcmd` のIDL(unitree_hg) | 現行SDKで一致 | CRC不一致・受け付けないなら SDK も更新 |

★**FSM idはファーム更新で変わった前例がある。**(1 DoF腰のG1で、v1.4.9
以降 state 501 が使えなくなった、という報告がある。)ボタンの効きは
`probe_loco.py` の読み取りで先に確かめること。

## 付録: 中止条件

次のいずれかが出たら、その日は方策を動かさない。

- フェーズAで `★指令が届いていません` が出た
- 引き継ぎに0.5秒以上かかる、または引き継ぎ直後の傾きが5度を超える
- 同じパターンで2回連続して同じ失敗をした(原因不明のまま3回目をやらない)
- モータ温度が明らかに上がって挙動が変わってきた
- 吊り具が使えない状況になった
